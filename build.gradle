plugins {
  id 'maven-publish'
  id 'java-library'
}

group = 'com.github.Nestorsoiza'
version = 'master-SNAPSHOT'
sourceCompatibility = '1.8'

repositories {
    jcenter()
}

dependencies {
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.Nestorsoiza'
            artifactId = 'Tickets'
            version = 'master-SNAPSHOT'

            from components.java
        }
    }
}